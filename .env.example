
# Flask
FLASK_SECRET_KEY=replace_with_random_32bytes
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=false

# MySQL
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DB=totp_app
MYSQL_USER=root
MYSQL_PASSWORD=your_password

# MSAL / Azure AD
MSAL_CLIENT_ID=your_client_id
MSAL_CLIENT_SECRET=your_client_secret
MSAL_TENANT_ID=your_tenant_id
MSAL_REDIRECT_URI=http://localhost:5000/auth/callback
MSAL_SCOPES=User.Read

# Encryption for TOTP secrets (Fernet key, base64 urlsafe)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOTP_ENC_KEY=
